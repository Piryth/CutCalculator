class Profil {

    //Attributs
    _poidsDeBase;
    _mgBase;
    _mbObj;

    //Constructeurs
    constructor(poidsDeBase, mgBase, mgObj) {
        this._poidsDeBase = poidsDeBase;
        this._mgBase = mgBase;
        this._mbObj = mgObj;
    }


    
}